# NarcoNet Watch Frontend

React/Vite frontend for the NarcoNet Watch dashboard.

Use `npm run dev` for development. `/api` calls are proxied to Flask on port 5000.
Use `npm run build` to create `dist/`; Flask serves that directory in production.
